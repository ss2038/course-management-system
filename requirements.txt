Flask==2.3.2
Flask-SQLAlchemy==3.0.3        # For SQLAlchemy ORM integration with Flask
SQLAlchemy==2.0.19             # Core SQLAlchemy package
Flask-Mail==0.9.1              # For sending emails
Flask-Login==0.6.3             # Optional, if you're managing user sessions more explicitly
Werkzeug==2.3.2                # Password hashing and other utilities
email-validator==1.3.1         # Email validation for forms
psycopg2-binary==2.9.6         # PostgreSQL driver, if you're using PostgreSQL
Jinja2==3.1.2                  # Templating engine (comes with Flask)
WTForms==3.0.1                 # For forms handling (if using with Flask-WTF)
itsdangerous==2.1.2            # For secure cookies and sessions
click==8.1.3                   # For CLI support (comes with Flask)
MarkupSafe==2.1.2              # HTML escaping (comes with Flask)
